{
  "name": "tfg-topics",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.10-bullseye",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.analysis.extraPaths": [
          "backend-ingestion/src",
          "backend-processing/src",
          "frontend-web/src"
        ],
        "python.testing.pytestEnabled": true,
        "python.testing.unittestEnabled": false,
        "python.formatting.provider": "none",
        "editor.formatOnSave": true
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-python.black-formatter",
        "ms-python.isort",
        "ms-python.mypy-type-checker",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "containerEnv": {
    "PYTHONPATH": "/workspaces/${localWorkspaceFolderBasename}/backend-ingestion/src:/workspaces/${localWorkspaceFolderBasename}/backend-processing/src:/workspaces/${localWorkspaceFolderBasename}/frontend-web/src"
  },
  "postCreateCommand": "python -m pip install --upgrade pip && pip install black isort flake8 mypy pre-commit pytest pytest-cov pytest-mock && pre-commit install",
  "postAttachCommand": {
    "install-ingestion-deps": "echo \"Install ingestion deps with: pip install -r backend-ingestion/requirements.txt -r backend-ingestion/requirements-dev.txt\"",
    "install-processing-deps": "echo \"Install processing deps with: pip install -r backend-processing/requirements.txt -r backend-processing/requirements-dev.txt\"",
    "install-frontend-deps": "echo \"Install frontend deps with: pip install -r frontend-web/requirement.txt\""
  }
}
